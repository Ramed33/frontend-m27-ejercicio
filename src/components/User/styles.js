import styled from "styled-components";

const TitleUser = styled.p`
    font-size: large;
    font-weight: 800;
`;

const NameUser = styled.b`
    font-size: large;
    font-weight: 700;
`;

export {
    TitleUser,
    NameUser
}